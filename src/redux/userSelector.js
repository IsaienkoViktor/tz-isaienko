export const selectUsers = (state) => state.users.item;

export const selectFilter = (state) => state.filter.name;

export const selectItems = (state) => state.items.items;

export const selectVisibleItems = (state) => state.items.visibleItems;
